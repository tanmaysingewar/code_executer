#include <stdio.h>

int main() {
    int num1, num2;

    // Input
    scanf("%d %d", &num1, &num2);

    // Calculate the sum
    int sum = num1 + num2;

    // Output
    printf("%d\n", sum);

    return 0;
}